package org.superheroes.config;

/** marker annotation only used to prevent IDEs from complaining about unused resolver methods (those annotated as <code>&#64;Source</code>). */
public @interface Resolver {}
